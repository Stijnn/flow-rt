import { ScrollArea } from "@/components/ui/scroll-area";

export const EditorScriptsOverviewPage = () => {
  return (
    <ScrollArea className="flex-1 py-3 px-3 h-full w-full">
        <div className="flex-1 flex flex-col h-full bg-[color-mix(in_srgb,var(--card),black_35%)] rounded-lg">
            <div>
                
            </div>
        </div>
    </ScrollArea>
  );
};
