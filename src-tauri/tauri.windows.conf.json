{
  "build": {
    "beforeDevCommand": "python prebuild.py --clean && npm run dev",
    "beforeBuildCommand": "python prebuild.py --clean --release && npm run build"
  },
  "bundle": {
    "active": true,
    "targets": ["msi", "nsis"],
    "resources": ["modules/*.dll"]
  }
}
  